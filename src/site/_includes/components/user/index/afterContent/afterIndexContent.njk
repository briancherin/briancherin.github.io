<h2>Recently updated</h2>
<ul>

{%- for note in collections.note | slice(0, 7) -%}
  <li>
    <a href="{{ note.url }}">{{ note.title or note.fileSlug }}</a>
    <small class="human-date" data-date="{{ note.data.updated }}"></small>
  </li>
{%- endfor -%}
</ul>
