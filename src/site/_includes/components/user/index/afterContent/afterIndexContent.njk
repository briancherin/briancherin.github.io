<h2>Recently updated</h2>

{# collect only items with an updated date, skip homepage #}
{%- set published = [] -%}
{%- for note in collections.note -%}
<script>console.log("test6")</script>
  {%- if note.url != "/" and note.data.updated -%}
    {%- set published = (published.push(note), published) -%}
    <script>console.log("test7" + "{{published}}")</script>
  {%- endif -%}
{%- endfor -%}

{# sort descending by updated using built-in sort filter + reverse trick #}
{%- set published = published | sort(false, false, "data.updated") | reverse -%}

<ul>
{%- for note in published  -%}
<script>console.log("test8")</script>
  <li>
    <a href="{{ note.url }}">{{ note.data.title or note.fileSlug }}</a>
    <span style="opacity:0.7;">
      — {{ note.data.updated | humanDate }}
    </span>
  </li>
{%- endfor -%}
</ul>



<ul>

{%- for note in collections.note -%}
<script>console.log("test3")</script>
<script>console.log("{{note.title}} - {{note.fileSlug}}")</script>

  <li>
    <a href="{{ note.url }}">{{ note.title or note.fileSlug }}</a>
    <span style="opacity:0.7;">
        — {{ note.data.updated | humanDate }}
    </span>
  </li>
{%- endfor -%}
</ul>




<hr style="margin-top: 3rem; margin-bottom: 1rem; opacity: 0.5; border-top: 1px solid #ccc;">

<p style="text-align: center; font-size: 0.9rem; color: #777;">
  <a href="https://forms.gle/B5rkQCYMBTHrR6fDA" target="_blank" rel="noopener noreferrer"
     style="text-decoration: underline;">
    Send me a comment!
  </a>
</p>
